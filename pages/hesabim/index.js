import React from 'react';
import LayoutTree from "../../components/layout/LayoutTree";
const Index = (props) => {
    return (
        <LayoutTree>

        </LayoutTree>
    );
};

export default Index;
